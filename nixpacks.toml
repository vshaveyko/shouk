providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.build]
cmds = [
  "npm ci",
  "npx prisma generate",
  "npx prisma migrate deploy",
  "npm run build",
]

[start]
cmd = "npm run start"
